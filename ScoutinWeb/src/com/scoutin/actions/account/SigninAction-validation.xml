<!DOCTYPE validators PUBLIC "-//Apache Struts//XWork Validator 1.0.3//EN"
		"http://struts.apache.org/dtds/xwork-validator-1.0.3.dtd">
<validators>
	<field name="email">
	    <field-validator type="fieldexpression">
		    <param name="expression">!(email == null and authType == 0)</param> 
			<message key="error.account.signin.email.required">Please input a phone number</message>
		</field-validator>
		<field-validator type="email">
			<message key="error.account.signin.email">The email seems invalid</message>
		</field-validator>
	</field>
	
	<field name="phone">
		<field-validator type="fieldexpression">
		    <param name="expression">!(phone == null and authType == 3)</param> 
			<message key="error.account.signin.phone.required">Please input a phone number</message>
		</field-validator>
	</field>
	
	<field name="password">
	    <field-validator type="stringlength">
	        <param name="minLength">8</param>
			<message key="error.account.signin.password.short">The password should be at least ${minLength} characters</message>
		</field-validator>
	</field>
	
	<field name="authType">
		<field-validator type="int">
			<param name="min">0</param>
			<param name="max">3</param>
			<message>authType must be between ${min} and ${max}, current value is ${authType}</message>
		</field-validator>
	</field>
</validators>